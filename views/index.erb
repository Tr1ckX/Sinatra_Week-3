<div style='border: 3px dashed red'>
  <% if @visitor %>
   Hello, <%= @visitor %>!
  <% else %>
    <form action="/">
      My name is <%= @name %>.
      What's your name?
      <input type="text" name="name">
      <input type="submit">
    </form>
  <% end %>
  <img src='/images/kitten.png'> #could be 'kitten.png', keep in the public folder
</div>                           #to use as addition at the actual url's end
